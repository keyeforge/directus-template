import type { DeepPartial, ProAliasToken } from '@ant-design/pro-components'

/** Pro Components 扩展 Token（ProConfigProvider token） */
export const proThemeToken: DeepPartial<ProAliasToken> = {
  layout: {
    pageContainer: {
      paddingInlinePageContainerContent: 16,
      paddingBlockPageContainerContent: 16,
    },
  },
}
